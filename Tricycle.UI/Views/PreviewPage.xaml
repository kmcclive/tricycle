<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Tricycle.UI.Controls"
             x:Class="Tricycle.UI.Views.PreviewPage"
             BackgroundColor="{StaticResource BackgroundColor}">
    <StackLayout Padding="10, 10, 10, 20">
        <FlexLayout JustifyContent="End" Grid.Row="1" Grid.Column="1">
            <local:PlatformButton Source="Images/close.png" Command="{Binding CloseCommand}" />
        </FlexLayout>
        <FlexLayout JustifyContent="Center"
                    AlignItems="Center"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="FillAndExpand">
            <ActivityIndicator IsVisible="{Binding IsSpinnerVisible}" IsRunning="True" />
            <Image IsVisible="{Binding IsImageVisible}"
                   Source="{Binding CurrentImageSource}" />
        </FlexLayout>
        <FlexLayout JustifyContent="SpaceAround">
            <local:PlatformButton Source="Images/previous.png" Command="{Binding PreviousCommand}" />
            <local:PlatformButton Source="Images/next.png" Command="{Binding NextCommand}" />
        </FlexLayout>
    </StackLayout>
</ContentPage>
